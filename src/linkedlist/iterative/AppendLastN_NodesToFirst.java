package linkedlist.iterative;

import linkedlist.Node;

/**
 * AppendLastNToFirst ================== 
 * You have been given a singly linked
 * list of integers along with an integer 'N'. Write a function to append the
 * last 'N' nodes towards the front of the singly linked list and returns the
 * new head to the list.
 * 
 * @author Durgesh
 *
 */
public class AppendLastN_NodesToFirst {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}
	/**
	 * Method appends last n nodes to front of the linkedlist and returns the new head node
	 * @param head
	 * @param n, number of nodes to be moved from last to front of linkedlist
	 * @return
	 */
	public static Node<Integer> appendLastNToFirst(Node<Integer> head, int n){
		return head;
	}
}
